/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.assemblyai.api.resources.transcripts.types;

import com.assemblyai.api.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonValue;
import com.fasterxml.jackson.core.JsonParseException;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import java.io.IOException;
import java.util.Objects;

@JsonDeserialize(using = TranscriptWebhookNotification.Deserializer.class)
public final class TranscriptWebhookNotification {
    private final Object value;

    private final int type;

    private TranscriptWebhookNotification(Object value, int type) {
        this.value = value;
        this.type = type;
    }

    @JsonValue
    public Object get() {
        return this.value;
    }

    public <T> T visit(Visitor<T> visitor) {
        if (this.type == 0) {
            return visitor.visit((TranscriptReadyNotification) this.value);
        } else if (this.type == 1) {
            return visitor.visit((RedactedAudioResponse) this.value);
        }
        throw new IllegalStateException("Failed to visit value. This should never happen.");
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof TranscriptWebhookNotification && equalTo((TranscriptWebhookNotification) other);
    }

    private boolean equalTo(TranscriptWebhookNotification other) {
        return value.equals(other.value);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.value);
    }

    @java.lang.Override
    public String toString() {
        return this.value.toString();
    }

    public static TranscriptWebhookNotification of(TranscriptReadyNotification value) {
        return new TranscriptWebhookNotification(value, 0);
    }

    public static TranscriptWebhookNotification of(RedactedAudioResponse value) {
        return new TranscriptWebhookNotification(value, 1);
    }

    public interface Visitor<T> {
        T visit(TranscriptReadyNotification value);

        T visit(RedactedAudioResponse value);
    }

    static final class Deserializer extends StdDeserializer<TranscriptWebhookNotification> {
        Deserializer() {
            super(TranscriptWebhookNotification.class);
        }

        @java.lang.Override
        public TranscriptWebhookNotification deserialize(JsonParser p, DeserializationContext ctxt) throws IOException {
            Object value = p.readValueAs(Object.class);
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, TranscriptReadyNotification.class));
            } catch (IllegalArgumentException e) {
            }
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, RedactedAudioResponse.class));
            } catch (IllegalArgumentException e) {
            }
            throw new JsonParseException(p, "Failed to deserialize");
        }
    }
}
