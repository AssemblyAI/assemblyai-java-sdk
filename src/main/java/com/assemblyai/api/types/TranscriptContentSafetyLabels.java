/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.assemblyai.api.types;

import com.assemblyai.api.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.util.ArrayList;
import java.util.List;
import java.util.Objects;

@JsonInclude(JsonInclude.Include.NON_EMPTY)
@JsonDeserialize(builder = TranscriptContentSafetyLabels.Builder.class)
public final class TranscriptContentSafetyLabels {
    private final AudioIntelligenceModelStatus status;

    private final List<ContentSafetyLabelResult> results;

    private TranscriptContentSafetyLabels(AudioIntelligenceModelStatus status, List<ContentSafetyLabelResult> results) {
        this.status = status;
        this.results = results;
    }

    /**
     * @return Will be either success, or unavailable in the rare case that the Content Safety Labels model failed.
     */
    @JsonProperty("status")
    public AudioIntelligenceModelStatus getStatus() {
        return status;
    }

    @JsonProperty("results")
    public List<ContentSafetyLabelResult> getResults() {
        return results;
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof TranscriptContentSafetyLabels && equalTo((TranscriptContentSafetyLabels) other);
    }

    private boolean equalTo(TranscriptContentSafetyLabels other) {
        return status.equals(other.status) && results.equals(other.results);
    }

    @Override
    public int hashCode() {
        return Objects.hash(this.status, this.results);
    }

    @Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static StatusStage builder() {
        return new Builder();
    }

    public interface StatusStage {
        _FinalStage status(AudioIntelligenceModelStatus status);

        Builder from(TranscriptContentSafetyLabels other);
    }

    public interface _FinalStage {
        TranscriptContentSafetyLabels build();

        _FinalStage results(List<ContentSafetyLabelResult> results);

        _FinalStage addResults(ContentSafetyLabelResult results);

        _FinalStage addAllResults(List<ContentSafetyLabelResult> results);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements StatusStage, _FinalStage {
        private AudioIntelligenceModelStatus status;

        private List<ContentSafetyLabelResult> results = new ArrayList<>();

        private Builder() {}

        @Override
        public Builder from(TranscriptContentSafetyLabels other) {
            status(other.getStatus());
            results(other.getResults());
            return this;
        }

        /**
         * <p>Will be either success, or unavailable in the rare case that the Content Safety Labels model failed.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @Override
        @JsonSetter("status")
        public _FinalStage status(AudioIntelligenceModelStatus status) {
            this.status = status;
            return this;
        }

        @Override
        public _FinalStage addAllResults(List<ContentSafetyLabelResult> results) {
            this.results.addAll(results);
            return this;
        }

        @Override
        public _FinalStage addResults(ContentSafetyLabelResult results) {
            this.results.add(results);
            return this;
        }

        @Override
        @JsonSetter(value = "results", nulls = Nulls.SKIP)
        public _FinalStage results(List<ContentSafetyLabelResult> results) {
            this.results.clear();
            this.results.addAll(results);
            return this;
        }

        @Override
        public TranscriptContentSafetyLabels build() {
            return new TranscriptContentSafetyLabels(status, results);
        }
    }
}
