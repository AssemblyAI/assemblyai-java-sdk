/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.assemblyai.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class RealtimeTranscriptType {
    public static final RealtimeTranscriptType FINAL_TRANSCRIPT =
            new RealtimeTranscriptType(Value.FINAL_TRANSCRIPT, "FinalTranscript");

    public static final RealtimeTranscriptType PARTIAL_TRANSCRIPT =
            new RealtimeTranscriptType(Value.PARTIAL_TRANSCRIPT, "PartialTranscript");

    private final Value value;

    private final String string;

    RealtimeTranscriptType(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof RealtimeTranscriptType
                        && this.string.equals(((RealtimeTranscriptType) other).string));
    }

    @Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case FINAL_TRANSCRIPT:
                return visitor.visitFinalTranscript();
            case PARTIAL_TRANSCRIPT:
                return visitor.visitPartialTranscript();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static RealtimeTranscriptType valueOf(String value) {
        switch (value) {
            case "FinalTranscript":
                return FINAL_TRANSCRIPT;
            case "PartialTranscript":
                return PARTIAL_TRANSCRIPT;
            default:
                return new RealtimeTranscriptType(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        PARTIAL_TRANSCRIPT,

        FINAL_TRANSCRIPT,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitPartialTranscript();

        T visitFinalTranscript();

        T visitUnknown(String unknownType);
    }
}
